name: Keep Render Services Alive

on:
  schedule:
    - cron: '1-59/14 0-16 * * *'
  workflow_dispatch:

jobs:
  ping-services:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - name: 'recommendation-ml'
            url: 'https://tramcamxuc-monu-a73k.onrender.com/health'
          - name: 'service-social'
            url: 'https://tramcamxuc-monu.onrender.com/health'
      max-parallel: 2
    
    steps:
      - name: Ping ${{ matrix.service.name }}
        run: |
          echo "🔄 Pinging ${{ matrix.service.name }}..."
          curl -s -o /dev/null -w "Status: %{http_code}\n" "${{ matrix.service.url }}" || echo "⚠️  Service might be sleeping"
        continue-on-error: true

      - name: Wait before next request
        run: sleep 2
